---
interface Props {
  src: string;
  alt: string;
  scrollable?: boolean;
}

const { src, alt, scrollable = true } = Astro.props;
---

<div class="phone-mockup">
  <div class:list={["phone-screen", { scrollable }]}>
    <img 
      src={src} 
      alt={alt} 
      loading="lazy"
      decoding="async"
    />
  </div>
</div>

<style>
  .phone-mockup {
    position: relative;
    /* Marco exterior cherry con opacidad suave */
    background-color: var(--color-primary);
    border-radius: 44px;
    padding: 3px;
    aspect-ratio: 9 / 19.5;
    max-height: 600px;
    width: auto;
    box-shadow:
      /* Sombra difusa principal */
      0 24px 60px rgba(103, 6, 38, 0.22),
      /* Sombra dura cercana para profundidad */
      0 6px 16px rgba(0, 0, 0, 0.18),
      /* Borde interior iluminado */
      inset 0 0 0 1px rgba(255, 255, 255, 0.12);
  }

  /* Capa intermedia entre el borde cherry y la pantalla */
  .phone-mockup::before {
    content: '';
    position: absolute;
    inset: 3px;
    border-radius: 42px;
    background: #111;
    z-index: 0;
  }

  .phone-screen {
    position: relative;
    z-index: 1;
    width: 100%;
    height: 100%;
    border-radius: 40px;
    overflow: hidden;
    background-color: #000;
  }

  .phone-screen.scrollable {
    overflow-y: auto;
  }

  .phone-screen:not(.scrollable) img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .phone-screen.scrollable img {
    width: 100%;
    height: auto;
    display: block;
  }

  /* Scrollbar styling */
  .phone-screen.scrollable::-webkit-scrollbar {
    width: 3px;
  }

  .phone-screen.scrollable::-webkit-scrollbar-track {
    background: transparent;
  }

  .phone-screen.scrollable::-webkit-scrollbar-thumb {
    background-color: var(--color-primary);
    border-radius: 4px;
  }

  .phone-screen.scrollable {
    scrollbar-width: thin;
    scrollbar-color: var(--color-primary) transparent;
  }
</style>
