---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import BrowserMockup from '../../components/BrowserMockup.astro';
import MobileShowcase from '../../components/MobileShowcase.astro';

const project = {
  title: 'Xplora',
  subtitle: 'Rediseño de plataforma para descubrir cafeterías en Lima según el perfil del usuario',
  year: '2025',
  client: 'Xplora',
  role: 'UX/UI & Frontend',
  duration: '1 mes',
  tools: ['React', 'Next.js', 'Tailwind CSS', 'GitHub', 'Claude', 'ChatGPT'],
  tags: ['UX Research', 'UI Design', 'Frontend Development', 'Rediseño'],
  liveUrl: 'https://xplora.chat/',
  overview: 'Xplora es una plataforma que ayuda a los usuarios de Lima a encontrar la cafetería perfecta según su perfil. El cliente ya tenía una web avanzada y un diseño previo en Figma, así que prescindimos de una etapa de diseño nueva: hice el research, definí los perfiles de usuario, replantée la arquitectura y los filtros, y ejecuté todo el rediseño directamente en el código del repositorio existente.',
  challenge: 'El principal reto fue continuar un código que no escribí yo, entender las decisiones previas y hacer mejoras significativas desde adentro sin romper lo que ya funcionaba. Además, la plataforma trataba a todos los usuarios igual sin considerar que cada uno llega con una necesidad distinta — trabajar, socializar o encontrar café de especialidad. Reorganizar esa experiencia basándome en datos reales fue el núcleo del proyecto.',
  goals: [
    'Identificar los tres perfiles principales de usuario',
    'Rediseñar el sistema de filtros según necesidades reales',
    'Mejorar la navegación para reducir el tiempo de decisión',
    'Implementar el rediseño directamente en el repositorio'
  ],
  process: [
    {
      phase: 'Research',
      title: 'Entender antes de tocar el código',
      description: 'El cliente ya tenía una web funcionando y un diseño previo en Figma. Antes de intervenir revisé el comportamiento de los usuarios, analicé qué estaba fallando en la experiencia actual y estudié plataformas similares. El hallazgo clave fue que los usuarios no buscaban "una cafetería" en abstracto, sino un contexto muy específico — trabajar, reunirse con amigos o encontrar café de especialidad. Eso definió todo el rediseño.',
      image: '/images/projects/xplora/antes.webp',
      mockup: true,
      url: 'xplora.chat',
      imageLabel: 'Versión original'
    },
    {
      phase: 'Definición',
      title: 'Perfilado y rediseño de filtros',
      description: 'Con los tres perfiles identificados redefiní el sistema de filtros para que reflejaran necesidades reales: wifi, nivel de ruido, tipo de café, capacidad para grupos. Los filtros dejaron de ser atributos genéricos y pasaron a ser herramientas orientadas a cada tipo de usuario.',
      image: '/images/projects/xplora/home.png',
      mockup: false
    },
    {
      phase: 'Implementación',
      title: 'Rediseño directo en el repositorio',
      description: 'Con la arquitectura y los filtros definidos, trabajé el rediseño directamente en el repositorio del cliente en React y Next.js, sin archivo Figma previo. El proceso fue iterativo: ajustaba, revisaba en el navegador y refinaba. La colaboración con el equipo se gestionó a través de GitHub con revisiones de código y pull requests. El entregable fue el producto final en producción, con mejor navegación, diseño más coherente y filtros basados en datos reales de usuarios.',
      image: '/images/projects/xplora/desarrollo.webp',
      mockup: false
    }
  ],
  testimonial: {
    quote: 'Julisa se encargó del rediseño total de la web y el resultado fue increíble. Partiendo de nuestra idea, ella logró que los colores y el estilo tuvieran muchísima más coherencia y un enfoque súper moderno. Propuso estructuras nuevas para la información que hicieron que todo se vea impecable y sea fácil de navegar. La ejecución fue de diez; entregó un trabajo con una calidad y un profesionalismo que superó nuestras expectativas.',
    author: 'Edgar Chambilla',
    role: 'Founder & CEO, Xplora'
  },
  desktopShowcase: {
    mainPages: [
      { src: '/images/projects/xplora/home.webp', label: 'Home', alt: 'Xplora — Home rediseñada', url: 'xplora.chat' },
      { src: '/images/projects/xplora/loginweb.png', label: 'Login', alt: 'Xplora — Pantalla de login', url: 'xplora.chat/login' }
    ]
  },
  mobileShowcase: [
    {
      title: 'Home',
      mobile: '/images/projects/xplora/homemobile.png'
    },
    {
      title: 'Cafeterías',
      mobile: '/images/projects/xplora/cafeteriamobile.png'
    },
    {
      title: 'Login',
      mobile: '/images/projects/xplora/loginmobile.png'
    }
  ],
  nextProject: {
    slug: 'intense',
    title: 'Intense Viajes'
  }
};
---

<Layout title={`${project.title} | Caso de Estudio`} description={project.overview}>
  <Header />
  
  <main>
    <!-- Hero Section -->
    <section data-hero class="relative min-h-screen flex flex-col justify-end px-6 md:px-12 lg:px-24 pb-16 pt-32 overflow-hidden">
      <!-- Back link -->
      <a href="/work" class="reveal group inline-flex items-center gap-2 text-[var(--color-text-muted)] hover:text-[var(--color-primary)] transition-colors mb-12 relative z-10" data-delay="0">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 transition-transform duration-300 group-hover:-translate-x-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16l-4-4m0 0l4-4m-4 4h18" />
        </svg>
        <span class="text-sm uppercase tracking-wider underline-grow">Volver a proyectos</span>
      </a>

      <div class="max-w-7xl relative z-10">
        <div class="reveal flex flex-wrap gap-2 mb-6" data-delay="0.1">
          {project.tags.map((tag, index) => (
            <span class="tag hover-lift" style={`animation-delay: ${index * 0.05}s`}>{tag}</span>
          ))}
        </div>

        <h1 class="reveal font-display text-5xl md:text-6xl lg:text-7xl xl:text-8xl font-semibold mb-6 leading-[1.1]" data-delay="0.2">
          <span class="inline-block">{project.title}</span>
        </h1>

        <p class="reveal text-[var(--color-text-muted)] text-xl md:text-2xl max-w-3xl mb-12 leading-relaxed" data-delay="0.3">
          {project.subtitle}
        </p>

        <div class="reveal stagger-grid grid grid-cols-2 md:grid-cols-4 gap-8" data-delay="0.4">
          <div class="group">
            <p class="text-[var(--color-text-muted)] text-sm uppercase tracking-wider mb-2 transition-colors group-hover:text-[var(--color-primary)]">Cliente</p>
            <p class="font-medium text-lg">{project.client}</p>
          </div>
          <div class="group">
            <p class="text-[var(--color-text-muted)] text-sm uppercase tracking-wider mb-2 transition-colors group-hover:text-[var(--color-primary)]">Rol</p>
            <p class="font-medium text-lg">{project.role}</p>
          </div>
          <div class="group">
            <p class="text-[var(--color-text-muted)] text-sm uppercase tracking-wider mb-2 transition-colors group-hover:text-[var(--color-primary)]">Duración</p>
            <p class="font-medium text-lg">{project.duration}</p>
          </div>
          <div class="group">
            <p class="text-[var(--color-text-muted)] text-sm uppercase tracking-wider mb-2 transition-colors group-hover:text-[var(--color-primary)]">Año</p>
            <p class="font-medium text-lg">{project.year}</p>
          </div>
        </div>
      </div>

      <!-- Scroll indicator -->
      <div class="absolute bottom-8 left-1/2 -translate-x-1/2 reveal" data-delay="1">
        <div class="flex flex-col items-center gap-2">
          <span class="text-[var(--color-text-muted)] text-xs uppercase tracking-widest">Scroll</span>
          <div class="w-[1px] h-12 bg-[var(--color-border)] relative overflow-hidden">
            <div class="absolute top-0 left-0 w-full h-full bg-[var(--color-primary)] animate-scroll-line"></div>
          </div>
        </div>
      </div>
    </section>

    <!-- Hero Image -->
    <section class="px-6 md:px-12 lg:px-24 py-8 md:py-12">
      <div class="max-w-[1800px] mx-auto">
        <div class="reveal rounded-2xl overflow-hidden shadow-2xl" data-delay="0.2">
          <img 
            src="/images/projects/xplora/home.png"
            alt="Xplora — Vista principal del rediseño"
            class="w-full aspect-[16/9] object-cover object-top"
            loading="eager"
            decoding="async"
            fetchpriority="high"
          />
        </div>
      </div>
    </section>

    <!-- Overview -->    <section class="px-6 md:px-12 lg:px-24 py-24 border-t border-[var(--color-border)] relative overflow-hidden">
      <div class="absolute top-0 right-0 w-96 h-96 bg-[var(--color-secondary)]/5 rounded-full blur-3xl -translate-y-1/2 translate-x-1/2"></div>

      <div class="max-w-7xl mx-auto grid grid-cols-1 lg:grid-cols-12 gap-12 lg:gap-24 relative z-10">
        <div class="lg:col-span-4">
          <p class="reveal text-[var(--color-text-muted)] text-sm uppercase tracking-[0.3em] mb-4 flex items-center gap-2" data-delay="0">
            <span class="w-8 h-[1px] bg-[var(--color-primary)]"></span>
            Resumen
          </p>
          <h2 class="reveal font-serif text-3xl md:text-4xl lg:text-5xl font-medium italic" data-delay="0.1">
            Sobre el proyecto
          </h2>
        </div>
        <div class="lg:col-span-8">
          <p class="reveal text-lg md:text-xl leading-relaxed text-[var(--color-text-muted)] mb-8" data-delay="0.2">
            {project.overview}
          </p>
          
          <div class="reveal" data-delay="0.3">
            <p class="text-sm uppercase tracking-wider text-[var(--color-text-muted)] mb-4">Herramientas</p>
            <div class="flex flex-wrap gap-3">
              {project.tools.map((tool, index) => (
                <span class="tag hover-lift glow" style={`animation-delay: ${index * 0.1}s`}>{tool}</span>
              ))}
            </div>
          </div>
        </div>
      </div>

      <div class="section-divider max-w-7xl mx-auto mt-24"></div>
    </section>

    <!-- Challenge -->
    <section class="relative px-6 md:px-12 lg:px-24 py-24 bg-[var(--color-primary)] overflow-hidden">
      <!-- Decorative elements -->
      <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[800px] rounded-full border border-[#FDF8F3]/5"></div>
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] rounded-full border border-[#FDF8F3]/5"></div>
      </div>

      <div class="max-w-[1600px] mx-auto grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-24 relative z-10">
        <!-- Left: title + description -->
        <div>
          <p class="reveal text-[var(--color-secondary)] text-sm uppercase tracking-[0.3em] mb-4 flex items-center gap-3" data-delay="0">
            <span class="w-8 h-[1px] bg-[var(--color-secondary)]"></span>
            El reto
          </p>
          <h2 class="reveal font-serif text-3xl md:text-4xl lg:text-5xl font-medium text-[#FDF8F3] mb-8 italic" data-delay="0.1">
            Tres usuarios,<br/>una misma plataforma
          </h2>
          <p class="reveal text-lg md:text-xl leading-relaxed text-[#FDF8F3]/80" data-delay="0.2">
            {project.challenge}
          </p>
        </div>

        <!-- Right: goals as animated pills -->
        <div class="space-y-4">
          {project.goals.map((goal, index) => (
            <div 
              class="reveal group flex items-center justify-between gap-6 px-6 py-5 bg-[#FDF8F3] rounded-full hover:bg-[var(--color-secondary)] transition-all duration-500 cursor-default hover-lift"
              data-delay={0.2 + index * 0.1}
            >
              <div class="flex items-center gap-4">
                <span class="flex-shrink-0 w-10 h-10 rounded-full bg-[var(--color-primary)]/10 text-[var(--color-primary)] flex items-center justify-center text-sm font-semibold group-hover:bg-[var(--color-primary)] group-hover:text-[#FDF8F3] transition-all duration-300 group-hover:scale-110">
                  {String(index + 1).padStart(2, '0')}
                </span>
                <span class="text-[var(--color-primary)] font-medium text-lg">{goal}</span>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Process -->
    <section class="px-6 md:px-12 lg:px-24 py-24 border-t border-[var(--color-border)] relative">
      <div class="absolute bottom-0 left-0 w-72 h-72 bg-[var(--color-primary)]/5 rounded-full blur-3xl"></div>

      <div class="max-w-7xl mx-auto relative z-10">
        <div class="text-center mb-20">
          <p class="reveal text-[var(--color-text-muted)] text-sm uppercase tracking-[0.3em] mb-4 flex items-center justify-center gap-3" data-delay="0">
            <span class="w-8 h-[1px] bg-[var(--color-primary)]"></span>
            Mi proceso
            <span class="w-8 h-[1px] bg-[var(--color-primary)]"></span>
          </p>
          <h2 class="reveal font-serif text-3xl md:text-4xl lg:text-5xl font-medium italic" data-delay="0.1">
            Del research al código
          </h2>
          <p class="reveal text-[var(--color-text-muted)] text-lg mt-4 max-w-2xl mx-auto" data-delay="0.2">
            Un proceso end-to-end: investigación de usuarios, perfilado, rediseño de filtros e implementación directa en el repositorio.
          </p>
        </div>

        <!-- Process steps - alternating layout -->
        <div class="space-y-32">
          {project.process.map((step, index) => (
            <div class={`grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-24 items-center`}>
              <div class={index % 2 === 1 ? 'lg:order-2' : ''}>
                <div class="reveal" data-delay="0">
                  <div class="inline-flex items-center gap-3 mb-4">
                    <span class="w-8 h-[1px] bg-[var(--color-primary)]"></span>
                    <span class="text-[var(--color-primary)] text-xs uppercase tracking-[0.3em]">{step.phase}</span>
                  </div>

                  <div class="flex items-center gap-4 mb-6">
                    <h3 class="font-serif text-2xl md:text-3xl lg:text-4xl font-medium italic">
                      {step.title}
                    </h3>
                  </div>

                  <p class="text-[var(--color-text-muted)] text-lg leading-relaxed">
                    {step.description}
                  </p>
                </div>
              </div>
              <div class={index % 2 === 1 ? 'lg:order-1' : ''}>
                <div class="reveal" data-delay="0.2">
                  {step.mockup ? (
                    <BrowserMockup
                      src={step.image}
                      alt={step.title}
                      label={step.imageLabel}
                      url={step.url}
                      height="420px"
                    />
                  ) : (
                    <div class="rounded-2xl overflow-hidden">
                      <img 
                        src={step.image}
                        alt={step.title}
                        class="w-full aspect-[4/3] object-cover object-top"
                        loading="lazy"
                      />
                    </div>
                  )}
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Desktop Showcase + Video -->
    <section class="py-24 border-t border-[var(--color-border)]">
      <div class="max-w-[1800px] mx-auto px-6 md:px-12 lg:px-16">
        <!-- Section Header -->
        <div class="text-center mb-16">
          <p class="reveal text-[var(--color-text-muted)] text-sm uppercase tracking-[0.3em] mb-4" data-delay="0">
            Diseño UI
          </p>
          <h2 class="reveal font-serif text-4xl md:text-5xl lg:text-6xl font-medium" data-delay="0.1">
            Pantallas Desktop
          </h2>
        </div>

        <!-- BrowserMockups -->
        <div class="space-y-24">
          <div>
            <h3 class="reveal font-serif text-2xl md:text-3xl font-medium mb-10 text-[var(--color-text)]" data-delay="0">
              Páginas principales
            </h3>
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
              {project.desktopShowcase.mainPages.map((page, index) => (
                <div class="reveal" data-delay={0.1 + index * 0.05}>
                  <BrowserMockup
                    src={page.src}
                    alt={page.alt}
                    label={page.label}
                    url={page.url}
                    height="500px"
                  />
                </div>
              ))}
            </div>
          </div>

        </div>
      </div>
    </section>

    <!-- Antes & Después -->
    <section class="py-24 bg-[var(--color-primary)] overflow-hidden">
      <div class="max-w-[1800px] mx-auto px-6 md:px-12 lg:px-16">
        <!-- Header centrado -->
        <div class="text-center mb-16">
          <p class="reveal text-white/60 text-sm uppercase tracking-[0.35em] mb-4" data-delay="0">
            El resultado
          </p>
          <h2 class="reveal font-serif text-5xl md:text-6xl lg:text-7xl font-medium text-white" data-delay="0.1">
            Antes &amp; Después
          </h2>
          <p class="reveal text-white/70 text-lg mt-5 max-w-xl mx-auto" data-delay="0.2">
            De una interfaz confusa a una experiencia fluida y personalizada.
          </p>
        </div>

        <!-- Comparación lado a lado -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-10">
          <!-- Antes -->
          <div class="reveal" data-delay="0.15">
            <p class="text-xs uppercase tracking-[0.3em] text-white/50 mb-4 text-center">Antes</p>
            <BrowserMockup
              src="/images/projects/xplora/antes.webp"
              alt="Versión original de Xplora antes del rediseño"
              label="Versión original"
              url="xplora.chat"
              height="480px"
            />
          </div>

          <!-- Después -->
          <div class="reveal" data-delay="0.2">
            <p class="text-xs uppercase tracking-[0.3em] text-white/50 mb-4 text-center">Después</p>
            <!-- Video mockup manual para mantener consistencia visual -->
            <div class="browser-mockup-video rounded-xl overflow-hidden shadow-2xl">
              <!-- Chrome bar -->
              <div class="flex items-center gap-3 px-4 py-3 bg-[var(--color-bg-secondary)] border-b border-[var(--color-border)]">
                <div class="flex gap-2">
                  <span class="w-3 h-3 rounded-full bg-[#ff5f56] opacity-70"></span>
                  <span class="w-3 h-3 rounded-full bg-[#ffbd2e] opacity-70"></span>
                  <span class="w-3 h-3 rounded-full bg-[#27c93f] opacity-70"></span>
                </div>
                <div class="flex-1 flex items-center gap-2 bg-white/60 rounded-md px-3 py-1.5 border border-[var(--color-border)]">
                  <svg class="w-3.5 h-3.5 text-[var(--color-text-muted)] shrink-0" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4zm0 10.99h7c-.53 4.12-3.28 7.79-7 8.94V12H5V6.3l7-3.11v8.8z"/>
                  </svg>
                  <span class="text-xs text-[var(--color-text-muted)] font-medium truncate">xplora.chat</span>
                </div>
              </div>
              <!-- Video: overflow hidden en el wrapper recorta las franjas negras -->
              <div style="height: 480px; overflow: hidden; position: relative;">
                <video
                  src="/images/projects/xplora/xplora.mp4"
                  autoplay
                  muted
                  loop
                  playsinline
                  style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 100%; height: 130%; object-fit: cover;"
                ></video>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Mobile Showcase -->
    <MobileShowcase
      title="Experiencia Mobile"
      subtitle="Responsive Design"
      screens={project.mobileShowcase}
    />

    <!-- Testimonial -->
    <section class="relative px-6 md:px-12 lg:px-24 py-32 border-t border-[var(--color-border)] overflow-hidden">
      <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] rounded-full bg-[var(--color-secondary)]/5 blur-3xl"></div>
      
      <div class="max-w-4xl mx-auto text-center relative z-10">
        <div class="reveal" data-delay="0">
          <span class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-[var(--color-primary)]/10 mb-8">
            <svg class="w-8 h-8 text-[var(--color-primary)]" fill="currentColor" viewBox="0 0 24 24">
              <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z"/>
            </svg>
          </span>
        </div>
        <blockquote class="reveal font-serif text-2xl md:text-3xl lg:text-4xl font-medium leading-relaxed mb-10 italic text-[var(--color-text-muted)]" data-delay="0.1">
          "{project.testimonial.quote}"
        </blockquote>
        <div class="reveal" data-delay="0.2">
          <div class="inline-flex items-center gap-4">
            <div class="w-12 h-[1px] bg-[var(--color-border)]"></div>
            <div>
              <p class="font-medium text-lg text-[var(--color-text-muted)]">{project.testimonial.author}</p>
              <p class="text-[var(--color-text-muted)] text-sm">{project.testimonial.role}</p>
            </div>
            <div class="w-12 h-[1px] bg-[var(--color-border)]"></div>
          </div>
        </div>
      </div>
    </section>

    <!-- Next Project -->
    <section class="border-t border-[var(--color-border)] relative overflow-hidden">
      <a href={`/work/${project.nextProject.slug}`} class="group block px-6 md:px-12 lg:px-24 py-24 transition-colors duration-500 hover:bg-[var(--color-secondary)]">
        <div class="absolute inset-0 bg-[var(--color-secondary)] transform scale-y-0 origin-bottom transition-transform duration-700 ease-out group-hover:scale-y-100 -z-10"></div>
        
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row md:items-center md:justify-between gap-8 relative z-10">
          <div>
            <p class="text-[var(--color-text-muted)] text-sm uppercase tracking-[0.3em] mb-4 flex items-center gap-2 group-hover:text-[var(--color-primary)]/70 transition-colors duration-500">
              <span class="w-8 h-[1px] bg-current"></span>
              Siguiente proyecto
            </p>
            <h3 class="font-serif text-3xl md:text-4xl lg:text-5xl xl:text-6xl font-medium italic group-hover:text-[var(--color-primary)] transition-colors duration-500">
              {project.nextProject.title}
            </h3>
          </div>
          <div class="flex-shrink-0">
            <div class="w-20 h-20 rounded-full border-2 border-[var(--color-border)] inline-flex items-center justify-center group-hover:border-[var(--color-primary)] group-hover:bg-[var(--color-primary)] transition-all duration-500 group-hover:scale-110 leading-none">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 flex-none transition-all duration-500 group-hover:translate-x-2 group-hover:text-[#FDF8F3]" fill="none" viewBox="0 0 24 24" stroke="currentColor" style="display:block">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
              </svg>
            </div>
          </div>
        </div>
      </a>
    </section>
  </main>

  <Footer />
</Layout>

<script>
  // Auto-scroll para el mockup "Antes" en la sección Antes & Después
  function initBeforeScroll() {
    // El BrowserMockup genera un div con clase browser-content.with-scroll
    // Seleccionamos el que contiene la imagen antes.webp
    const allScrollables = document.querySelectorAll<HTMLElement>('.browser-content.with-scroll');
    let beforeContainer: HTMLElement | null = null;

    allScrollables.forEach((el) => {
      const img = el.querySelector<HTMLImageElement>('img');
      if (img && img.src.includes('antes')) {
        beforeContainer = el;
      }
    });

    if (!beforeContainer) return;

    const container = beforeContainer as HTMLElement;
    let direction = 1;
    let animId: number;
    let paused = false;

    // Velocidad de scroll en px por frame (~60fps)
    const SPEED = 2;
    // Pausa en ms al llegar al extremo
    const PAUSE_MS = 1200;

    function step() {
      if (paused) return;

      const maxScroll = container.scrollHeight - container.clientHeight;
      if (maxScroll <= 0) return;

      container.scrollTop += SPEED * direction;

      if (container.scrollTop >= maxScroll - 1) {
        paused = true;
        setTimeout(() => {
          direction = -1;
          paused = false;
          animId = requestAnimationFrame(step);
        }, PAUSE_MS);
        return;
      }

      if (container.scrollTop <= 0) {
        paused = true;
        setTimeout(() => {
          direction = 1;
          paused = false;
          animId = requestAnimationFrame(step);
        }, PAUSE_MS);
        return;
      }

      animId = requestAnimationFrame(step);
    }

    // Iniciar solo cuando la sección entra en viewport
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            animId = requestAnimationFrame(step);
          } else {
            cancelAnimationFrame(animId);
          }
        });
      },
      { threshold: 0.2 }
    );

    // Observar la sección padre
    const section = container.closest('section');
    if (section) observer.observe(section);

    // Pausar al hacer hover (el usuario quiere ver algo)
    container.addEventListener('mouseenter', () => {
      paused = true;
      cancelAnimationFrame(animId);
    });
    container.addEventListener('mouseleave', () => {
      paused = false;
      animId = requestAnimationFrame(step);
    });
  }

  // Esperar a que la imagen cargue para tener el scrollHeight correcto
  document.addEventListener('DOMContentLoaded', () => {
    const img = document.querySelector<HTMLImageElement>('img[src*="antes"]');
    if (img && img.complete) {
      initBeforeScroll();
    } else if (img) {
      img.addEventListener('load', initBeforeScroll);
    }
  });
</script>
